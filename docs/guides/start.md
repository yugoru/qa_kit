# Руководство для студентов: установка окружения и запуск проекта QA Kit

Это памятка, которая помогает полностью развернуть локальное окружение **с нуля**, даже если на компьютере студента вообще ничего не установлено.

---

# ✅ 1. Что нужно установить перед запуском

Все шаги выполняются **один раз**.
Если у вас уже что‑то установлено — просто пропустите соответствующий шаг.

## 1.1 Установите бесплатную IDE (редактор кода)

Можно выбрать любую:

### Вариант 1 — Visual Studio Code (рекомендуем)

Скачать:

```
https://code.visualstudio.com/
```

### Вариант 2 — PyCharm Community

```
https://www.jetbrains.com/pycharm/download/
```

### Вариант 3 — VS Code + расширения Python

После установки VS Code установите дополнение Python:
**Extensions → Python → Install**.

---

# ✅ 2. Установите Git

Git нужен, чтобы загрузить проект.

Скачать для Windows:

```
https://git-scm.com/download/win
```

Проверить, что Git установился:

```
git --version
```

Если команда выводит версию — всё ок.

---

# ✅ 3. Установите Docker Desktop

Docker — это платформа, в которой будут запускаться все сервисы проекта.

Скачать:

```
https://www.docker.com/products/docker-desktop/
```

После установки:

1. Запустите Docker Desktop вручную.
2. Убедитесь, что он показывает **Running**.

Если Docker не запущен — проект не заработает.

---

# ✅ 4. Клонирование проекта QA Kit

Откройте терминал (PowerShell) и выполните:

```
git clone https://github.com/yugoru/qa_kit.git
```

После клонирования:

```
cd qa_kit
```

Теперь ваш проект находится на компьютере.

---

# ✅ 5. Запуск проекта (все сервисы сразу)

В корне проекта выполните:

```
docker compose up --build
```

При первом запуске загрузка образов может занять время.

После того как все сервисы поднимутся, вы увидите логи:

* `qa_api_rest` — API запущен
* `qa_postgres` — база данных готова
* `qa_kafka` — брокер событий запущен
* `qa_kafka_ui` — UI для просмотра событий в Kafka
* `qa_portal` — портал доступен

Чтобы остановить проект:

```
CTRL + C
```

или

```
docker compose down
```

---

# 📍 6. Полезные команды Docker

## 6.1 Посмотреть список работающих контейнеров

```
docker ps
```

## 6.2 Посмотреть логи сервиса (например, API)

```
docker logs qa_api_rest --tail 100
```

## 6.3 Перезапустить один сервис

```
docker restart qa_api_rest
```

## 6.4 Полностью остановить проект

```
docker compose down
```

## 6.5 Остановить проект и удалить данные PostgreSQL

```
docker compose down -v
```

> Используйте только если хотите начать с чистой базы данных.

---

# 🎯 7. Куда заходить после запуска

## 7.1 Портал проекта (главная точка входа)

```
http://localhost:8090
```

## 7.2 REST API (Swagger)

```
http://localhost:8000/docs
```

## 7.3 Проверить API

```
http://localhost:8000/health
```

## 7.4 Kafka UI (сообщения, топики, события)

```
http://localhost:8081
```

---

# 🧩 8. Что включает в себя проект

* **PostgreSQL** — база данных
* **REST API** — работа с заказами
* **Kafka (Redpanda)** — брокер сообщений
* **Kafka UI** — визуальный просмотр сообщений
* **WebSocket API** — двустороннее взаимодействие
* **Portal (frontend)** — удобная точка входа

Это полноценный мини-микросервисный стек, который даёт практику QA уровня middle+.

---

# 🎓 9. Что можно потренировать

* работу с Docker и docker-compose
* чтение логов, диагностику ошибок
* работу с базой данных через DBeaver
* тестирование REST API
* тестирование WebSockets
* тестирование Kafka сообщений
* end‑to‑end тесты
