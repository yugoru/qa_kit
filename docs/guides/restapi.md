# REST API: Инструкция

Эта памятка поможет протестировать REST API учебного сервиса **QA Kit**. Здесь собраны все основные запросы, их примеры и ожидаемые ответы.

---

## 1. Базовый URL

Все запросы отправляются на:

```
http://localhost:8000
```

Документация (Swagger):

```
http://localhost:8000/docs
```

---

# 📌 Основные REST ручки

## 2. Проверка здоровья сервиса

**GET /health**

```
GET http://localhost:8000/health
```

**Ответ:**

```
{"status": "ok"}
```

---

## 3. Создание заказа

**POST /orders**

**Запрос:**

```
POST http://localhost:8000/orders
Content-Type: application/json

{
  "customer": "Yulia"
}
```

**Ответ:**

```
{
  "id": 1,
  "customer": "Yulia",
  "status": "created"
}
```

---

## 4. Получить список всех заказов

**GET /orders**

```
GET http://localhost:8000/orders
```

**Ответ:**

```
[
  {
    "id": 1,
    "customer": "Yulia",
    "status": "created"
  }
]
```

---

## 5. Получить заказ по ID

**GET /orders/{id}**

```
GET http://localhost:8000/orders/1
```

---

# 📌 Управление статусами заказа

Допустимые переходы:

* created → paid / cancelled
* paid → shipped / cancelled
* shipped → delivered

## 6. Оплатить заказ

**POST /orders/{id}/pay**

```
POST http://localhost:8000/orders/1/pay
```

**Ответ:**

```
{
  "id": 1,
  "customer": "Yulia",
  "status": "paid"
}
```

---

## 7. Отправить заказ

**POST /orders/{id}/ship**

```
POST http://localhost:8000/orders/1/ship
```

---

## 8. Доставить заказ

**POST /orders/{id}/deliver**

```
POST http://localhost:8000/orders/1/deliver
```

---

## 9. Отменить заказ

**POST /orders/{id}/cancel**

```
POST http://localhost:8000/orders/1/cancel
```

### Ошибка при неправильном переходе

```
{
  "detail": "Cannot change status from 'delivered' to 'paid'"
}
```

---

# 🎓 Что можно протестировать

* корректность CRUD запросов
* статус-коды 200 / 400 / 404
* изменения статуса заказа
* соответствие REST ↔ WebSocket ↔ Kafka
* автогенерацию схем в Swagger
